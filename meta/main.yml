galaxy_info:
  author: Henryk Konsek
  description: Ensures that Docker engine is installed and running as a systemd service. Also installs Docker client.
  company: Red Hat

  license: Apache 2.0

  min_ansible_version: 1.2

  platforms:
  - name: Fedora
    versions:
    - 26 # 27, but Galaxy doesn't support it yet

  galaxy_tags: ["fedora", "docker"]